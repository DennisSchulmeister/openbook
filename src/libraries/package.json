{
  "name": "@openbook-studio/libraries",
  "private": true,
  "packageManager": "npm@11.x",
  "scripts": {
    "clean": "npm run clean:core && npm run clean:ls-compat",
    "build": "npm run build:core && npm run build:ls-compat",
    "lint": "npm run lint:core && npm run lint:ls-compat",
    "watch": "conc -n core,ls-compat -c auto \"npm run watch:core\" \"npm run watch:ls-compat\"",
    "clean:core": "cd core && npm run clean",
    "build:core": "cd core && npm run build",
    "lint:core": "cd core && npm run lint",
    "watch:core": "cd core && npm run watch",
    "clean:ls-compat": "cd ls-compat && npm run clean",
    "build:ls-compat": "cd ls-compat && npm run build",
    "lint:ls-compat": "cd ls-compat && npm run lint",
    "watch:ls-compat": "cd ls-compat && npm run watch"
  }
}
