name: "Run Full Test Suite"

on:
    workflow_call:

permissions:
    contents: write

jobs:
    run-full-test-suite:
        name: "Run full test suite"
        runs-on: ubuntu-latest

        services:
            redis:
                image: redis:8-alpine@sha256:2afba59292f25f5d1af200496db41bea2c6c816b059f57ae74703a50a03a27d0
                ports:
                    - 6379:6379

        steps:
            # Must be first, so that GH actions finds our local actions!
            - name: "Checkout code"
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
              with:
                  ref: ${{ github.head_ref || github.ref }}

            - name: "Set up CI environment"
              uses: ./.github/actions/setup-ci-environment
              with:
                  setup-python: "true"
                  setup-node: "true"

            - name: "Regenerate project SBOM"
              uses: ./.github/actions/generate-project-sbom
              with:
                  commit-changes: "false"
                  upload-artifact: "true"

            - name: "Run all checks and tests"
              run: |
                  npm run check
