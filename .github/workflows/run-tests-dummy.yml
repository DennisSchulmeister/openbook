name: "Run Dummy Test Suite"

on:
    workflow_call:

jobs:
    run-dummy-test-suite:
        name: "Run dummy test suite"
        runs-on: ubuntu-latest

        steps:
            - name: "Report skipped test suite"
              run: |
                  echo "No files in src/ changed. Skipping test suite."